#include <stdio.h>

int main(void)
{
    /* + addition
     - soustraction
     * multiplication
     / division
     % modulo (5 / 2 = 2 et 5 % 2 = 1)
     */

    /* int calcul = 6 + 4;
    printf("%d\n", calcul);

    a = a - x 
    => a -= x

    a = a * x 
    => a *= x
 */
    //ou printf("%d\n", 6 + 4);

    /*  int nb1 = 6;
    int nb2 = 3;

    printf("%d\n", nb1 + nb2); */

    int niveau_joueur = 1;

    // niveau apres xp

    niveau_joueur = niveau_joueur + 1; // = variable += x

    //incrementation de 1 : A+= 1 ou A-= 1 (fonctionne uniquement avec 1 en addition soustraction)
    // A++ ou A--
    // ++A ou --AA

    return 0;
}
